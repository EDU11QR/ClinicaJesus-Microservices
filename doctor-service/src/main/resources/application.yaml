spring:
  application:
    name: doctor-service

  datasource:
    url: jdbc:mysql://localhost:3306/clinicajesus_db
    username: root
    password: mysql123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/